name: Daily PDF to Excel Processor

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  process-pdfs:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.1

      - name: Set up Python
        uses: actions/setup-python@v6.1.0
        with:
          python-version: '3.13'
          check-latest: true 

      - name: Install dependencies
        run: |
          pip install pandas openpyxl pdfplumber

      - name: Download and Extract ZIP
        env:
          TARGET_URL: 'https://officeprotocoldoc.z19.web.core.windows.net/files/Zip_Files/Exchange_Protocols.zip' 
        run: |
          echo "Downloading from $TARGET_URL..."
          curl -L -o download.zip "$TARGET_URL"
           
          echo "Unzipping files..."
          # -j flattens the directory structure so all PDFs are in the root
          unzip -o -j download.zip "*.pdf" || echo "No PDFs found or warning issued"

      - name: Convert PDFs to Excel
        run: |
          cat <<EOF > process_pdfs.py
          import glob
          import pandas as pd
          import pdfplumber
          import os
          import re

          pdf_files = glob.glob("*.pdf")
          output_filename = "combined_pdf_data.xlsx"
          used_sheet_names = set()

          print(f"Found {len(pdf_files)} PDF files.")

          if not pdf_files:
              print("No PDF files found to process.")
              exit()

          # Initialize ExcelWriter with openpyxl engine
          with pd.ExcelWriter(output_filename, engine='openpyxl') as writer:
              for pdf_file in pdf_files:
                  try:
                      # 1. Extract content
                      text_content = ""
                      with pdfplumber.open(pdf_file) as pdf:
                          for page in pdf.pages:
                              page_text = page.extract_text()
                              if page_text:
                                  text_content += page_text + "\n"
                      
                      # 2. Create DataFrame (row-by-row structure like your example)
                      lines = text_content.split('\n')
                      df = pd.DataFrame(lines, columns=["Full Content"])

                      # 3. Generate Valid Excel Sheet Name
                      base_name = os.path.splitext(pdf_file)[0]
                      # Remove invalid Excel characters: : \ / ? * [ ]
                      clean_name = re.sub(r'[\\/*?:\[\]]', '', base_name)
                      
                      # Truncate to 31 chars (Excel limit)
                      sheet_name = clean_name[:31]

                      # Handle Duplicate Sheet Names
                      # If truncation results in a duplicate, append _1, _2, etc.
                      original_truncated = sheet_name
                      counter = 1
                      while sheet_name in used_sheet_names:
                          # Shorten further to make room for suffix
                          suffix = f"_{counter}"
                          max_len = 31 - len(suffix)
                          sheet_name = original_truncated[:max_len] + suffix
                          counter += 1
                      
                      used_sheet_names.add(sheet_name)

                      # 4. Write to Excel
                      df.to_excel(writer, sheet_name=sheet_name, index=False)
                      print(f"Processed: '{pdf_file}' into Sheet: '{sheet_name}'")

                  except Exception as e:
                      print(f"Error processing {pdf_file}: {e}")

          if os.path.exists(output_filename):
              print(f"Successfully created {output_filename}")
          else:
              print("Failed to create Excel file.")
          EOF
           
          python process_pdfs.py

      - name: Upload Excel Artifact
        uses: actions/upload-artifact@v6.0.0
        with:
          name: daily-pdf-report
          path: combined_pdf_data.xlsx
          retention-days: 5
